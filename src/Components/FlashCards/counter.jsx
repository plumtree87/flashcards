import React from 'react'
import { Card } from 'react-bootstrap';

const Counter = (props) => {

   
    return (
        <div>
      
       
        {props.count}
       

       </div>
    );
}

export default Counter;
